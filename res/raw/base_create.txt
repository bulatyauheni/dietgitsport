CREATE TABLE `base` (
  `_id` INTEGER PRIMARY KEY,
  `name` TEXT,
  `protein` NUMBER, 
  `fat` NUMBER, 
  `carbon` NUMBER,
 `caloricity` INTEGER ,
  `category_name` TEXT,
  `type` TEXT,
 `category` INTEGER ,
  `dish_id` INTEGER ,
  `description` TEXT);